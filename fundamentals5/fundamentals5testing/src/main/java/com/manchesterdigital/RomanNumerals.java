package com.manchesterdigital;

public class RomanNumerals {
     protected static String converstionToNumeral(Integer number){
         String result = "";
        String[] decimalPlace = number.toString().split("");
         for(int i = 0; i < decimalPlace.length; i++){
             if (Integer.valueOf(decimalPlace[i]) < 4){

             }
         }
        return result;
    }
}
//eevrythiong ended up a mess